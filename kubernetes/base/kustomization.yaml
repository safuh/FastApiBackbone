apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

# Create the real secret out-of-band (or through an approved secret manager).
# `secret.example.yaml` is documentation only and must not be applied.
