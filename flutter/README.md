# Flutter Client

This directory is reserved for the reference Flutter client and generator templates.

The backend remains independent of Flutter. The client will consume the OpenAPI contract generated by FastAPI and will provide a thin application shell rather than duplicate backend domain logic.

Implementation is tracked in Phase 5 of `docs/MILESTONES.md`.
