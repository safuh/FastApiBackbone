apiVersion: v1
kind: Secret
metadata:
  name: fastapi-backbone
stringData:
  # Replace before applying. Do not commit real credentials.
  DATABASE_URL: postgresql+asyncpg://USER:PASSWORD@HOST:5432/DATABASE
